# ===================================
#  Column Definitions
# ===================================

columns:
    name:
        label: general.translate::lang.locale.name
        searchable: yes

    code:
        label: general.translate::lang.locale.code
        searchable: yes

    is_default:
        label: general.translate::lang.locale.is_default
        type: switch

    is_enabled:
        label: general.translate::lang.locale.is_enabled
        type: switch
        invisible: true
